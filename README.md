# OmniCura

Agentic AI UI demo for drug repurposing & genomic discovery. Static mock data (BRCA1).

Run locally:

```powershell
npm install
$env:GEMINI_KEY="your-gemini-key"
npm run dev
```

Open http://localhost:5173
